spring:
  application:
    name: shopflow-backend

  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:54324/shopflow}
    username: ${DB_USERNAME:shopflow}
    password: ${DB_PASSWORD:shopflow}
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
    properties:
      hibernate:
        format_sql: true
        jdbc:
          time_zone: UTC

  flyway:
    enabled: true
    locations: classpath:db/migration
    group: true

  data:
    redis:
      host: ${REDIS_HOST:localhost}
      port: ${REDIS_PORT:63793}
      password: "${REDIS_PASSWORD:}"
      timeout: 2s

  cache:
    type: redis
    redis:
      key-prefix: "shopflow:"
      use-key-prefix: true
      cache-null-values: false
      time-to-live: 10m

  threads:
    virtual:
      enabled: true

  web:
    error:
      include-message: never
      include-binding-errors: never

server:
  port: ${SERVER_PORT:8080}

management:
  endpoints:
    web:
      exposure:
        include: health,info

springdoc:
  swagger-ui:
    path: /swagger-ui.html
  api-docs:
    path: /v3/api-docs

shopflow:
  security:
    jwt:
      issuer: ${JWT_ISSUER:shopflow-backend}
      secret: ${JWT_SECRET:shopflow-development-secret-change-me-32-bytes-minimum}
      access-token-ttl: ${JWT_ACCESS_TOKEN_TTL:PT1H}
    default-users:
      admin:
        email: ${SHOPFLOW_ADMIN_EMAIL:admin@shopflow.local}
        password: ${SHOPFLOW_ADMIN_PASSWORD:admin12345}
      customer:
        email: ${SHOPFLOW_CUSTOMER_EMAIL:customer@shopflow.local}
        password: ${SHOPFLOW_CUSTOMER_PASSWORD:customer12345}